Here is some regular text before the code block:

```
function greet(name) {
const message = "Hello, " + name;
console.log(message);
}
```

And here is a second code example:

```
import os
import sys
def main():
print("Hello World")
```

Regular text resumes after the code blocks above.
